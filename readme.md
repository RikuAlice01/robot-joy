# 🤖 Robot WiFi Joy Controller (ESP8266)

โปรเจคควบคุมหุ่นยนต์ / อุปกรณ์ผ่าน Wi-Fi  
โดยใช้ **D1 mini (ESP8266)** เป็น Access Point  
ควบคุมผ่านหน้าเว็บ **Joy Controller**

---

## 🧰 Hardware
- D1 mini (WeMos) / ESP8266
- Motor Driver (L298N / L9110 / Relay ฯลฯ)
- อุปกรณ์เสริม (LED / Relay / Servo)

---

## 💻 Software ที่ใช้

### โปรแกรมหลัก
- **Arduino IDE**
  - แนะนำเวอร์ชัน 2.x หรือ 1.8.x
  - ดาวน์โหลด: https://www.arduino.cc/en/software

---

## ⚙️ การติดตั้ง ESP8266 Board (สำคัญมาก)

### 1️⃣ เพิ่ม Board Manager URL
เปิด **Arduino IDE**  
ไปที่  
```

File → Preferences

```

ใส่ URL นี้ในช่อง **Additional Boards Manager URLs**

```

[https://arduino.esp8266.com/stable/package_esp8266com_index.json](https://arduino.esp8266.com/stable/package_esp8266com_index.json)

```

กด **OK**

---

### 2️⃣ ติดตั้ง ESP8266 Core
ไปที่  
```

Tools → Board → Boards Manager

```

ค้นหา:
```

esp8266

```

ติดตั้ง:
```

ESP8266 by ESP8266 Community

```

รอจนขึ้นคำว่า **Installed**

---

### 3️⃣ เลือก Board ให้ถูกต้อง
```

Tools → Board → ESP8266 Boards → LOLIN(WEMOS) D1 R2 & mini

```

---

### 4️⃣ ตั้งค่าอื่น ๆ (แนะนำ)
```

Upload Speed  : 115200
CPU Frequency: 80 MHz
Flash Size   : 4MB (FS:None)
Port         : COMx (ที่เป็น D1 mini)

````

---

## 📚 Library ที่ใช้
โปรเจคนี้ใช้เฉพาะ Library มาตรฐานของ ESP8266

```cpp
#include <ESP8266WiFi.h>
#include <ESP8266WebServer.h>
````

📌 **ไม่ต้องติดตั้งเพิ่ม**
มากับ ESP8266 core อยู่แล้ว

---

## 🔌 Pin Mapping (ค่าเริ่มต้น)

### Motor

| Function | D1 mini | GPIO   |
| -------- | ------- | ------ |
| IN1      | D5      | GPIO14 |
| IN2      | D6      | GPIO12 |

### Action Buttons

| Button | D1 mini | GPIO   |
| ------ | ------- | ------ |
| Y      | D1      | GPIO5  |
| X      | D2      | GPIO4  |
| B      | D7      | GPIO13 |
| A      | D8      | GPIO15 |

> ⚠️ GPIO15 (D8) ต้องเป็น LOW ตอนบูต (โค้ดนี้ปลอดภัย)

---

## 📡 Wi-Fi Configuration

```cpp
const char* ssid = "Robot-00";
const char* password = "12345678";
```

### ตั้งค่า Static IP

```cpp
IPAddress local_ip(192,168,1,1);
IPAddress gateway(192,168,1,1);
IPAddress subnet(255,255,255,0);

WiFi.softAPConfig(local_ip, gateway, subnet);
```

เข้าเว็บควบคุม:

```
http://192.168.1.1
```

---

## 🚀 วิธีใช้งาน (Getting Started)

1. เปิด Arduino IDE
2. เลือก Board: `LOLIN(WEMOS) D1 R2 & mini`
3. เชื่อมต่อ D1 mini กับคอมพิวเตอร์
4. เลือก Port ให้ถูกต้อง
5. อัปโหลดโค้ด
6. เชื่อมต่อ Wi-Fi ชื่อ `Robot-00`
7. เปิดเว็บ `192.168.1.1`
8. เริ่มควบคุมได้ทันที 🎮

---

## 🖥️ ตัวอย่าง Serial Log

```
CMD from browser: F
CMD from browser: S
CMD from browser: A_ON
CMD from browser: A_OFF
```
---

## 📜 License
RikuAlice01
MIT License
ใช้งานได้อิสระทั้งส่วนตัวและเชิงพาณิชย์
